# MKR syncing with Google Calendar

This script syncs the MKR schedule with a Google Calendar.

## Prerequisites
- Node.js 24+
- Google Calendar API credentials

## Usage

```bash
node index.js [create|remove|sync]
```

## Configuration
The configuration is stored in the `config.json` file.